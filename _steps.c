// Write a C program and run it in command prompt.
// first check version of gcc by "gcc -v"
// then write a C program
// then build the program in command prompt using "gcc first.c"
// build the program with name "gcc -o first.exe first.c"
// run the program "first.exe"

// Write a C program then create a makefile and run the program using that makefile.
// make a file named Makefile
// then declare like this
// first:
//     gcc -o first first.c